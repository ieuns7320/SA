# 현재 버전은 LLM을 전혀 호출하지 않으므로 ANTHROPIC_API_KEY는 필요 없다.

# 컨트랙트 주소로 입력할 때 검증된 소스코드를 가져오기 위한 키.
# https://etherscan.io/apis 에서 무료로 발급 가능 (요금 없음).
ETHERSCAN_API_KEY=

# 3단계(보류): Foundry 실행 바이너리 경로
FOUNDRY_BIN_PATH=forge
